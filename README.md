# HandGestureRecognition

This project is an advanced/customized hand gesture recognition pipeline based on MediaPipe landmarks, trained for 18 gesture classes.

## Requirements
- Python 3.10+
- pip

Install dependencies:
```bash
pip install -r requirements.txt
```
